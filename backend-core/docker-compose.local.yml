services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vimob_core
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - backend_core_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d vimob_core"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  backend_core_postgres:
